# Canonical workflow lives at repo root:
#   .github/workflows/azure-deploy-easylife.yml
# GitHub only runs workflows from the repository root .github folder.
# Keep this file as a pointer for discoverability inside the app tree.

name: Azure Deploy (moved)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Point to root workflow
        run: |
          echo "Use workflow 'Azure Deploy Easy Life' at .github/workflows/azure-deploy-easylife.yml"
          echo "This nested path is not executed by GitHub Actions."
